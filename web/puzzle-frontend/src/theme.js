import { createTheme } from '@mui/material/styles';

export default const ThemeOptions = createTheme({
    palette: {
	type: 'light',
	primary: {
	    main: '#054A91',
	},
	secondary: {
	    main: '#DB2763',
	},
    },
});
